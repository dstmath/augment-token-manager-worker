# Augment Token Manager 迁移指南

从 Cloudflare Worker + KV 迁移到 MySQL + 自托管服务器

## 📖 概述

本指南将帮助您将 Augment Token Manager 从 Cloudflare Worker 架构迁移到基于 Express.js + MySQL 的自托管服务器架构。

### 架构对比

| 组件 | Cloudflare Worker 版本 | 自托管服务器版本 |
|------|----------------------|----------------|
| **后端框架** | Cloudflare Worker | Express.js |
| **数据库** | Cloudflare KV | MySQL 8.0+ |
| **ORM** | 无 | Prisma |
| **部署平台** | Cloudflare | 自己的服务器 |
| **进程管理** | Cloudflare 自动管理 | PM2 |
| **反向代理** | Cloudflare 自动处理 | Nginx |
| **前端托管** | Cloudflare Worker | Nginx / CDN |

### 迁移优势

✅ **完全控制**: 完全掌控数据和服务器  
✅ **成本可控**: 无需担心 Cloudflare 的使用限制  
✅ **性能优化**: 可以根据需求调整服务器配置  
✅ **数据安全**: 数据存储在自己的服务器上  
✅ **功能扩展**: 更容易添加复杂功能  

## 🗂️ 项目结构

```
augment-token-manager-worker/
├── manager-worker/          # 原 Cloudflare Worker 后端（保留作为参考）
├── manager-server/          # 新的 Express.js 后端 ⭐
│   ├── prisma/             # Prisma 数据库配置
│   │   └── schema.prisma   # 数据库模型定义
│   ├── src/
│   │   ├── config/         # 配置文件
│   │   ├── middleware/     # 中间件
│   │   ├── routes/         # API 路由
│   │   ├── services/       # 业务逻辑
│   │   ├── scripts/        # 工具脚本
│   │   ├── types/          # TypeScript 类型
│   │   └── index.ts        # 入口文件
│   ├── .env.example        # 环境变量模板
│   ├── package.json
│   ├── ecosystem.config.cjs # PM2 配置
│   ├── README.md           # 后端文档
│   └── DEPLOYMENT.md       # 部署指南
├── manager-vue/            # Vue.js 前端
│   ├── src/
│   │   └── config/
│   │       └── api.ts      # API 配置 ⭐
│   ├── .env.example        # 环境变量模板 ⭐
│   └── MIGRATION.md        # 前端迁移指南 ⭐
└── MIGRATION_GUIDE.md      # 本文档
```

## 🚀 快速开始

### 前置要求

- Node.js >= 18.0.0
- MySQL >= 8.0
- PM2 (生产环境)
- Nginx (可选，推荐)

### 步骤 1: 准备 MySQL 数据库

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE augment_info CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 创建用户
CREATE USER 'augment_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON augment_info.* TO 'augment_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

### 步骤 2: 配置后端

```bash
cd manager-server

# 安装依赖
npm install

# 配置环境变量
cp .env.example .env
nano .env  # 编辑配置
```

关键配置：

```env
DATABASE_URL="mysql://augment_user:your_password@localhost:3306/augment_info"
PORT=6000
SESSION_SECRET=your-secret-key
USER_CREDENTIALS="admin:admin123"
CORS_ORIGIN=http://localhost:5173
```

### 步骤 3: 初始化数据库

```bash
# 生成 Prisma Client
npm run prisma:generate

# 运行数据库迁移
npm run prisma:migrate
```

### 步骤 4: 数据迁移（可选）

如果您有 Cloudflare KV 中的现有数据：

```bash
# 在 .env 中添加 Cloudflare 配置
CF_ACCOUNT_ID=your-account-id
CF_TOKENS_KV_NAMESPACE_ID=your-namespace-id
CF_SESSIONS_KV_NAMESPACE_ID=your-namespace-id
CF_API_TOKEN=your-api-token

# 运行迁移脚本
npm run db:migrate-from-kv
```

### 步骤 5: 启动后端

```bash
# 开发模式
npm run dev

# 生产模式
npm run build
npm start

# 或使用 PM2
pm2 start ecosystem.config.cjs
```

### 步骤 6: 配置前端

```bash
cd manager-vue

# 创建环境变量文件
cp .env.example .env.production

# 编辑配置
nano .env.production
```

配置内容：

```env
VITE_API_BASE_URL=http://localhost:6000
```

### 步骤 7: 测试

```bash
# 启动前端开发服务器
npm run dev
```

访问 `http://localhost:5173` 测试功能。

## 📚 详细文档

### 后端相关

- **[manager-server/README.md](manager-server/README.md)** - 后端项目文档
- **[manager-server/DEPLOYMENT.md](manager-server/DEPLOYMENT.md)** - 详细部署指南
- **[manager-server/prisma/schema.prisma](manager-server/prisma/schema.prisma)** - 数据库模型

### 前端相关

- **[manager-vue/MIGRATION.md](manager-vue/MIGRATION.md)** - 前端迁移指南
- **[manager-vue/src/config/api.ts](manager-vue/src/config/api.ts)** - API 配置

## 🔄 迁移流程图

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 准备工作                                                  │
│    - 安装 Node.js, MySQL, PM2                               │
│    - 创建 MySQL 数据库                                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. 后端设置                                                  │
│    - 安装依赖                                                │
│    - 配置环境变量                                            │
│    - 运行数据库迁移                                          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 数据迁移（可选）                                          │
│    - 配置 Cloudflare API 凭据                               │
│    - 运行迁移脚本                                            │
│    - 验证数据完整性                                          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 启动后端                                                  │
│    - 开发模式测试                                            │
│    - 生产模式部署                                            │
│    - 配置 PM2 自动重启                                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 前端配置                                                  │
│    - 更新 API 基础 URL                                       │
│    - 配置环境变量                                            │
│    - 构建生产版本                                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. 部署和测试                                                │
│    - 配置 Nginx 反向代理                                     │
│    - 配置 SSL 证书                                           │
│    - 全面功能测试                                            │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 核心功能对照

| 功能 | Cloudflare Worker | Express.js Server | 状态 |
|------|------------------|-------------------|------|
| 用户登录 | ✅ | ✅ | 已迁移 |
| 用户登出 | ✅ | ✅ | 已迁移 |
| Session 验证 | ✅ | ✅ | 已迁移 |
| Token 列表 | ✅ | ✅ | 已迁移 |
| Token 创建 | ✅ | ✅ | 已迁移 |
| Token 更新 | ✅ | ✅ | 已迁移 |
| Token 删除 | ✅ | ✅ | 已迁移 |
| 批量导入 | ✅ | ✅ | 已迁移 |
| Token 搜索 | ✅ | ✅ | 已迁移 |
| Token 统计 | ✅ | ✅ | 已迁移 |
| 分页查询 | ✅ | ✅ | 已迁移 |
| 速率限制 | ✅ | ✅ | 已迁移 |
| CORS 处理 | ✅ | ✅ | 已迁移 |

**注意**: 以下功能需要额外迁移（如需要）：
- OAuth 授权流程
- Email 服务集成
- Session 导入功能
- Credit 消费追踪
- Token 验证功能
- Token 分享功能

## 📊 数据库模型

### 核心表结构

```sql
-- 用户表
users (
  id, username, email, password, role, 
  isActive, createdAt, updatedAt
)

-- 会话表
sessions (
  id, sessionId, userId, expiresAt, 
  ipAddress, userAgent, createdAt
)

-- Token 表
tokens (
  id, tenantUrl, accessToken, portalUrl, emailNote,
  banStatus, portalInfo, shareInfo, isShared, authSession,
  createdBy, createdAt, updatedAt
)
```

详细模型定义请查看 `manager-server/prisma/schema.prisma`。

## 🛠️ 常用命令

### 后端

```bash
# 开发
npm run dev

# 构建
npm run build

# 启动
npm start

# PM2 管理
pm2 start ecosystem.config.cjs
pm2 stop augtoken-manager-server
pm2 restart augtoken-manager-server
pm2 logs augtoken-manager-server

# 数据库
npm run prisma:generate    # 生成 Prisma Client
npm run prisma:migrate     # 运行迁移
npm run prisma:studio      # 打开数据库管理界面
npm run db:migrate-from-kv # 从 KV 迁移数据
```

### 前端

```bash
# 开发
npm run dev

# 构建
npm run build

# 预览
npm run preview
```

## ✅ 迁移检查清单

### 准备阶段
- [ ] 服务器已准备（Ubuntu/CentOS/Debian）
- [ ] Node.js 18+ 已安装
- [ ] MySQL 8.0+ 已安装并配置
- [ ] PM2 已全局安装
- [ ] Nginx 已安装（可选）

### 后端迁移
- [ ] 后端依赖已安装
- [ ] 环境变量已配置
- [ ] 数据库已创建
- [ ] Prisma 迁移已运行
- [ ] 数据已从 KV 迁移（如需要）
- [ ] 后端服务已启动
- [ ] API 端点测试通过

### 前端迁移
- [ ] API 配置已更新
- [ ] 环境变量已配置
- [ ] CORS 配置正确
- [ ] 前端已构建
- [ ] 静态文件已部署

### 生产部署
- [ ] PM2 进程管理已配置
- [ ] Nginx 反向代理已配置
- [ ] SSL 证书已安装
- [ ] 防火墙规则已设置
- [ ] 数据库备份已配置
- [ ] 日志轮转已配置
- [ ] 监控已设置

### 测试验证
- [ ] 用户登录功能正常
- [ ] Token CRUD 操作正常
- [ ] 分页和搜索功能正常
- [ ] 批量操作功能正常
- [ ] 性能测试通过
- [ ] 安全测试通过

## 🐛 故障排查

### 常见问题

1. **数据库连接失败**
   - 检查 DATABASE_URL 配置
   - 确认 MySQL 服务运行中
   - 验证用户权限

2. **CORS 错误**
   - 检查后端 CORS_ORIGIN 配置
   - 确认前端 API_BASE_URL 正确
   - 重启后端服务

3. **PM2 进程崩溃**
   - 查看日志：`pm2 logs`
   - 检查内存使用
   - 调整 PM2 配置

4. **前端 API 请求失败**
   - 检查网络请求（浏览器 DevTools）
   - 验证 API 端点可访问
   - 检查认证 token

## 📞 获取帮助

- **后端文档**: [manager-server/README.md](manager-server/README.md)
- **部署指南**: [manager-server/DEPLOYMENT.md](manager-server/DEPLOYMENT.md)
- **前端迁移**: [manager-vue/MIGRATION.md](manager-vue/MIGRATION.md)
- **数据库模型**: [manager-server/prisma/schema.prisma](manager-server/prisma/schema.prisma)

## 🎉 完成

恭喜！您已成功将 Augment Token Manager 迁移到自托管服务器！

如有任何问题，请查看相关文档或检查日志文件。

